import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free AI Feasibility Report — KDR, Dual Occ, Granny Flat | AusBuildCircle',
  description: 'Get an instant AI feasibility report for your suburb. Council rules, realistic costs, approval timelines, and next steps — free, no signup required.',
}

export default function FeasibilityLayout({ children }: { children: React.ReactNode }) {
  return children
}
