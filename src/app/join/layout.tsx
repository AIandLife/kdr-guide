import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'List Your Business — Join the KDR Specialist Network | AusBuildCircle',
  description: 'Register as a verified KDR professional. Reach homeowners actively planning knockdown rebuilds in your area. Free listing for builders, planners, designers, and trades.',
}

export default function JoinLayout({ children }: { children: React.ReactNode }) {
  return children
}
