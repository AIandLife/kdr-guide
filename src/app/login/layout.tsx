import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Sign In | AusBuildCircle 澳洲建房圈',
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
